# 没带出来的东西（以及为什么）

这个项目是从一个**真的有人在住**的私人项目里剥出来的。有些东西留在了那边。
写在这儿，免得你以为是 bug 或者以为自己装漏了。

## 一、所有真实内容

真实的聊天记录、记忆、日记、照片、录音、通话存档 —— 一个字节都没有。

**而且没有「脱敏版」。** 脱敏过的真实资料还是真实资料。
项目里的示例数据是**专门新写的虚构内容**，不对应任何真人 ——
每个 demo 页面上都写着这一句，免得有人当真。

**原应用里那个 AI 伙伴的名字也没带进产品。** 积木的代码和默认值里没有写死的角色名，
要显示名字的地方一律走文件顶上那个 `NAME` 常量，默认值是中性的「伙伴」。
名字只在 [UPSTREAM.md](UPSTREAM.md) 交代来历的那一段里出现过一次。

## 二、密钥与生产配置

API key、数据库连接串、域名、隧道 token、部署脚本里的真实路径 —— 都没有。
`.env.example` 里是空壳，填你自己的。

> 顺带一条从原项目学到的教训：**别把 token 写进命令行**，`ps` 就能看见。
> 用环境变量文件或者你系统的密钥库。

## 三、桌宠的角色形象（机制在，素材不在 —— 我们只指路）

界面上有个能拖动的小挂件位置。**机制全都在**：拖动、记住位置、按状态换姿势、CSS 动画驱动。
**素材一张都没有**，而且这是故意的。

原项目用的那只，来自一个 AGPL-3.0 的粉丝项目，角色本身是另一家公司的吉祥物 ——
自己玩没问题，跟着开源项目分发不行。两层问题叠着。

所以做成了**指路**，不是分发：

| 装的时候你勾了 | 会发生什么 |
|---|---|
| 「要一只」 | 装机制，**代办里自动出现一条**：去哪个仓库拿、素材放哪、注意什么 |
| 「我自己画」 | 装机制，代办里给你命名规则。GIF、SVG 都行，随便传 |
| 都没勾（默认） | 不装。以后想加了，设置里还有入口 |

★ 我们只对那一只指了路。**你完全可以不要桌宠，或者以后自己加一个别的。**

## 四、上一代的界面

原项目前后做过三代皮。这里只有第三代。
前两代还挂在原项目里给老用户用，那些代码引了一个非 MIT 的动画库，
第三代已经不用它了，所以一并没带。

## 五、一部分「学别人的」功能，默认指向上游

凡是参考、改造、适配别的开源项目做出来的功能，都独立成了可选的适配包，
**默认推荐你先去原项目装**，来源和许可证见 `UPSTREAM.md`。
这边的适配层只是给「想省事、愿意接进同一个界面」的人用的。

## 六、还有一些留在了私家版

有些东西不适合公开，就留在那边了，也没有开关。
不是忘了，是判断过之后决定不带。
